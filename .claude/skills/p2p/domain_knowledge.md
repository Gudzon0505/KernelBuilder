DOMAIN_V8C v8C.1 — Domain Knowledge Reference. Adds project-specific context. Two frameworks: React 19/TypeScript and Kotlin/Coroutines. YAML format for agent prompt integration.

React 19: 18 core principles ("UI = f(state)"), decision trees for state management/effects, performance/accessibility/security checklists, anti-pattern detection.

Kotlin: null safety, coroutines, Flow/StateFlow patterns, multiplatform (KMP), sealed classes. Anti-patterns: !! operators, type-safety violations, brittle tests.

Checklists: architecture, state management, performance, TypeScript strictness, security (HTTPS, CSP, no client secrets), accessibility (WCAG, keyboard navigation).
