P2P v8C.2 Skill Manifest. Meta-prompt system for Claude that generates optimized prompts, orchestrates 8 specialized agents (QUORUM), manages large tasks via SCOPE.HELM, and adapts to multiple LLMs. Version v8C.2, developed for Claude Opus 4.7 and Sonnet 4.6. Supports 11 slash commands (/p2p, /p2p-quorum, /p2p-scope, /p2p-teacher, etc.). Two deployment methods: plugin method (Claude Code) and manual installation (API/Chat). Seven invariant checks passed: YAML formatting, API strings, deadlines, error handling, version metadata, XML compatibility, agent architecture.
